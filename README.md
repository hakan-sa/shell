##
This program simulates a simple command-line shell that can execute commands with input and output redirection. It includes features for changing directories and running processes with redirection.

## How to run
make
./shell
